#include <stdio.h>
/**
 * main- will print a statement
 * Return: 0 if finished correct not otherwise
 */
int main(void)
{
	printf("\"with proper grammar, but the outcome is a piece of art,");
	return (0);
}
